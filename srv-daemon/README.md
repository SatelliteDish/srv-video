# srv-daemon
srv-daemon is a simple Actix Web server built to handle srv-compatible requests. Right now, this is only serving feed files and videos but later will handle things like handling comments, ratings, and counting basic analytics like view counts.
