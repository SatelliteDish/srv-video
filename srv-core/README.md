# srv-core
This crate defines the shared protocol between a srv-host and srv-client. Currently only the RSS section is implemented, but this includes both the standard RSS fields and the extension. Later this will also include the shape of the JSON command payloads. This is the spec that will need to be matched by any system interoperable with any srv-host or srv-client.
