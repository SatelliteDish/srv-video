# srv-gui
srv-gui is a all-in-one GUI built to manage both hosting and watching videos. Right now this is not functional. srv-gui is built using `iced`, to give a clean UI cross-platform **without being a WebApp**. This means significantly better performance on systems with limited resources, especially when doing other tasks that require memory.
